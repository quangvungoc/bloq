<% if !current_user.nil? && current_user.admin? %>
  <%= link_to "delete", entry, method: :delete,                                 
  data: { confirm: "You sure?" },        
  title: entry.title %>
<% end %>